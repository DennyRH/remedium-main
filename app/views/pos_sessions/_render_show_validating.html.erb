<section class="app-papper row justify-content-center">
  <div class="col-12 col-md-8 col-lg-7 text-center">
    <% if @pos_session.missing_money > 0 %>
      <div class="mt-5">
        <h2 class="mt-3  text-danger">
          Dinero Faltante:
          <%= @pos_session.type_of_currency %>
          <%= @pos_session.missing_money %>
        </h2>
        <h5>
          Finaliza el cierre dejando un comentario para una revisión futura.
        </h5>
        <div class="my-4" data-controller="arching">
          <%= bootstrap_form_for(@pos_session,
              data: { arching_target: "formConfirmArchingValidating" }) do |f| %>
            <div class="row justify-content-center text-center">
              <input
                type="hidden" value="false" name="arching_forced"
                data-arching-target="archingForcedValue"
              >
              <div class="col-12">
                <%= f.text_area :observation, rows: 3, label: "Observación" %>
              </div>
              <div class="col-6 col-md-4">
                <button
                  type="button" class="btn btn-primary"
                  data-action="click->arching#setForceClosedSession"
                >
                  Cerrar Caja
                </button>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</section>
